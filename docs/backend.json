
{
  "entities": {
    "task": {
      "title": "Task",
      "description": "A single task item",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the task"
        },
        "description": {
          "type": "string",
          "description": "The description of the task"
        },
        "completed": {
          "type": "boolean",
          "description": "Whether the task is completed"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The creation timestamp of the task"
        }
      },
      "required": [
        "title",
        "completed",
        "createdAt"
      ]
    },
    "contactSubmission": {
      "title": "Contact Form Submission",
      "description": "Represents a single submission from the contact form.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the person submitting the form."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the person."
        },
        "message": {
          "type": "string",
          "description": "The content of the message."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The creation timestamp of the submission."
        },
        "attachmentUrl": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the uploaded attachment in Firebase Storage."
        },
        "attachmentName": {
            "type": "string",
            "description": "The original name of the attachment."
        }
      },
      "required": ["name", "email", "message", "createdAt"]
    },
    "imageRecord": {
      "title": "Image Record",
      "description": "Stores information about an original and transformed image.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who uploaded the image."
        },
        "originalImageUrl": {
          "type": "string",
          "format": "uri",
          "description": "URL of the original uploaded image in Firebase Storage."
        },
        "transformedImageUrl": {
          "type": "string",
          "format": "uri",
          "description": "URL of the AI-transformed image in Firebase Storage."
        },
        "originalFileName": {
          "type": "string",
          "description": "The original file name of the uploaded image."
        },
        "prompt": {
          "type": "string",
          "description": "The prompt used for the image transformation."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "The creation timestamp of the record."
        }
      },
      "required": [
        "userId",
        "originalImageUrl",
        "transformedImageUrl",
        "originalFileName",
        "prompt",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "/users/{userId}/tasks/{taskId}": {
      "schema": {
        "$ref": "#/entities/task"
      },
      "description": "Stores tasks for each user."
    },
    "/dth_contactMessages/{messageId}": {
      "schema": {
        "$ref": "#/entities/contactSubmission"
      },
      "description": "Stores submissions from the contact form."
    },
    "/imageRecords/{recordId}": {
        "schema": {
            "$ref": "#/entities/imageRecord"
        },
        "description": "Stores records of original and transformed images."
    }
  }
}
